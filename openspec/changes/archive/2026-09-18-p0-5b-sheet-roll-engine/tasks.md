## 1. Resolver dei bersagli a valore singolo

- [x] 1.1 Implementare in `server/character-sheet-roll-resolver.mjs` la lettura del bersaglio `ability:<key>`, `saving-throw:<key>`, `skill:<key>`, `initiative`, `tool:<id>` a partire dallo stato live della scheda e da `shared/dnd-rules.mjs`, restituendo un gruppo `{ count: 1, sides: 20, modifier }` o un errore quando l'input non è interpretabile; verificare con test unitari ciascun tipo di bersaglio con punteggio, competenza/livello e bonus vari noti, e il caso di bonus vari non interpretabile.
- [x] 1.2 Aggiungere la risoluzione di `hit-dice` (dado del tipo scelto nel riquadro, senza modificatore, tiro singolo non doppio) e `death-saves` (`1d20` senza modificatore), rifiutando `hit-dice` quando il tipo non è scelto o i dadi rimanenti sono a zero; verificare con test i tre casi: tipo scelto con dadi rimanenti, tipo non scelto, dadi rimanenti a zero.
- [x] 1.3 Rifiutare la risoluzione quando l'utente richiedente non supera `policy.canRead` sulla scheda del bersaglio, senza generare alcun dado; verificare con test che un Adventurer diverso dal proprietario e dal Master riceva un rifiuto.

## 2. Tiro doppio per i bersagli 1d20, resolver dell'attacco e del suo danno

- [x] 2.1 Tirare due `1d20` indipendenti (stesso modificatore, nessuno scartato) per ogni bersaglio la cui formula è `1d20 + modificatore` — caratteristica, tiro salvezza, abilità, iniziativa, strumento, tiro di attacco, salvataggio contro morte — senza alcuna modalità vantaggio/svantaggio dichiarata dal client; verificare con un test che il log riporti entrambi i valori naturali e che il dado vita resti un tiro singolo.
- [x] 2.2 Implementare la risoluzione di `attack:<id>` (solo il tiro di attacco, due `1d20 + bonus di attacco`) e di `attack-damage:<id>` (uno o due blocchi di danno indipendenti, il primo sempre attivo dalla Fase A) come bersagli separati, a partire dai campi dell'editor e da `computeAttackBonus`/`computeDamageModifier`; verificare con test un attacco con un solo blocco di danno attivo e uno con entrambi i blocchi attivi.
- [x] 2.3 Dichiarare `critical: true` sul log del tiro di attacco quando almeno uno dei due `1d20` naturali raggiunge `critRange`; sul bersaglio `attack-damage:<id>` accettare `critical` dichiarato dal client e raddoppiare i dadi di ciascun blocco di danno attivo quando `true`, lasciando invariato il modificatore; verificare con test un tiro naturale forzato sopra e sotto la soglia e un danno critico con uno e con due blocchi attivi.
- [x] 2.4 Restituire il blocco del tiro salvezza dell'attacco come metadato dichiarativo (caratteristica e CD) sulla voce di log del tiro di attacco, senza generare un tiro `1d20` per quella CD; verificare con test che nessun dado venga generato per il blocco del tiro salvezza.

## 3. Endpoint di tiro con sorgente dalla scheda

- [x] 3.1 Estendere `POST /api/battle-map/rolls` per accettare `{ source: { sheetId, target }, visibility?, critical? }` in alternativa a `formula`, instradando `source` al resolver del bersaglio invece di `parseRollFormula`; verificare con test un tiro con `source` e uno con `formula` che restano entrambi funzionanti.
- [x] 3.2 Riusare il generatore già introdotto per il tiro libero di P0.3 per ciascun dado generato dal resolver; verificare con test che la visibilità segreta nasconda l'intera voce, comprese le eventuali `parts`, agli altri Player.
- [x] 3.3 Estendere il record di log con il campo opzionale `parts` (una voce per blocco di danno quando un attacco ha due blocchi attivi, con `critical` quando raddoppiati), con `savingThrow` quando presente sul tiro di attacco, e con `source: { sheetId, target }` per collegare la voce al bersaglio d'origine; mantenere `formula`/`rolls`/`keptRolls`/`total`/`modifier` popolati come oggi per i bersagli a valore singolo e per il danno con un solo blocco attivo; verificare con un test che un consumatore che legga solo i campi di primo livello continui a funzionare sui tiri semplici.
- [x] 3.4 Collegare personaggio e azione a ogni voce di log generata da un bersaglio della scheda (per esempio nome del personaggio ed etichetta «Prova di Furtività» o «Attacco — Spada corta»); verificare con un test che due voci originate da bersagli diversi della stessa scheda siano distinguibili.
- [x] 3.5 Rifiutare con un errore leggibile, senza aggiungere alcuna voce al log, un bersaglio non interpretabile, un dado vita senza tipo scelto o senza dadi rimanenti, il danno di un attacco senza alcun blocco di danno attivo, o un tiro sui salvataggi contro morte quando il terzo pallino di successo o fallimento è già pieno; verificare ciascun caso con un test che controlli anche l'assenza di una nuova voce di log.

## 4. Effetti collaterali come patch della scheda

- [x] 4.1 Dopo un tiro accettato dal riquadro dei dadi vita, costruire e applicare tramite la funzione di patch già usata da `CharacterSheetService` un `set` che decrementa di uno `character.hitDice.remaining`, usando la versione live corrente come `baseVersion`; verificare con un test di service che i dadi rimanenti calino di uno e che la patch sia distribuita via SSE come le altre.
- [x] 4.2 Dopo un tiro accettato sui salvataggi contro morte, applicare allo stesso modo l'incremento del pallino di successo (primo dei due `1d20` da 10 a 20) o di fallimento (da 1 a 9) corrispondente; verificare con un test che il contatore aumenti di uno, resti fra 0 e 3 e che un terzo pallino raggiunto blocchi ulteriori tiri finché non viene azzerato a mano.
- [x] 4.3 Verificare che un fallimento nella validazione della patch dell'effetto collaterale interrompa la richiesta prima che qualunque voce raggiunga il log, mantenendo tiro e patch atomici dal punto di vista del client.

## 5. Interazione di tiro nel client

- [x] 5.1 Aggiungere un gestore di click delegato in `CharacterTab.tsx` sui nodi `data-roll-source` esistenti (e sul nuovo `attack-damage:<id>` sulla colonna del danno di ogni riga di attacco) che invia subito il tiro, con visibilità pubblica; nessuna modale precede il tiro; un elemento interattivo (input, select, button) dentro il bersaglio intercetta il click prima che raggiunga il bersaglio, così modificare un campo non avvia mai un tiro; verificare manualmente che il click su ciascuno dei nove bersagli produca un tiro.
- [x] 5.2 Dedurre dal log condiviso, senza scelta manuale, se applicare il critico quando si tira il danno di un attacco dalla scheda: cercare la voce di log più recente per lo stesso `attack:<id>` e leggerne `critical`; verificare che un danno tirato subito dopo un attacco critico raddoppi i dadi e che un danno tirato senza un attacco critico recente non li raddoppi.
- [x] 5.3 Continuare a esporre il tiro libero di P0.3 senza modifiche di comportamento mentre i bersagli della scheda sono attivi; verificare che aprire la scheda non nasconda né disattivi i controlli del pannello dei dadi.

## 6. Log e presentazione

- [x] 6.1 Aggiornare il rendering della tab Chat + Dadi (il feed nella sidebar, non la modale `DiceLogModal` mai aperta dall'interfaccia) per mostrare un bersaglio `1d20` come due totali affiancati con evidenza del 20/1 naturale, il danno di un attacco con i dadi separati da un `+` quando critico invece che sommati in silenzio, e render invariato per un tiro libero di P0.3; verificare visivamente un attacco critico seguito dal suo danno raddoppiato.
- [x] 6.2 Mostrare nel log personaggio e azione per ogni voce originata dalla scheda, distinguendo un'abilità da un attacco anche quando entrambi provengono dallo stesso personaggio, e rendere cliccabile il nome di un tiro di attacco per tirarne di nuovo il danno (applicando il critico se quel tiro di attacco lo aveva dichiarato); verificare che due voci ravvicinate restino leggibili e distinguibili.

## 7. Verifica integrata e documentazione

- [x] 7.1 Eseguire `npm test`, `npm run build` e `npm run docs:check` e correggere ogni regressione; verificare che i tre comandi terminino con successo. Verificato anche end-to-end contro il server reale (login, tiro, rifiuto per permessi, decremento dadi vita, tiro di attacco e danno critico) oltre ai test automatici.
- [x] 7.2 Aggiornare i router `Docs/ai` backend e frontend della scheda (`Docs/ai/backend/character_sheet_service_and_realtime.md`, `Docs/ai/frontend/character_sheet_client_and_window.md`) con il resolver dei bersagli, il tiro doppio per i bersagli 1d20, il danno come bersaglio separato con critico dedotto dal client, il formato di log e gli effetti collaterali applicati come patch; verificare con `npm run docs:check` che la documentazione resti raggiungibile e coerente.
- [x] 7.3 Aggiornare `FEATURES_VTT.md` marcando come completate le voci Players/Master/Sistema di P0.5 Fase B coperte da questa change, lasciando esplicitamente aperte quelle rimandate a P0.6, P0.8 e P0.11.
- [ ] 7.4 Eseguire lo scenario manuale con Master e due Player in browser distinti: click su un'abilità e su un tiro salvezza con doppio `1d20` visibile nel log; tiro di un attacco con critico forzato (o ripetuto fino a un 20 naturale) seguito dal tiro del danno dalla scheda e dal log, osservando il raddoppio dei dadi in entrambi i casi; tiro di un dado vita fino a esaurimento e verifica del rifiuto oltre lo zero; tre fallimenti contro morte che bloccano ulteriori tiri; verificare che nessun bersaglio escluso (Percezione passiva, righe della tab Incantesimi) produca un tiro. La visibilità segreta dei tiri della scheda resta da progettare in una fase successiva e non fa parte di questo scenario.
