## 1. Modulo condiviso delle regole 5e

- [x] 1.1 Creare `shared/dnd-rules.mjs` con modificatore di caratteristica, bonus di competenza dal livello e la tabella che associa ogni abilità alla propria caratteristica; verificare con test unitari i valori di confine del modificatore (punteggi 1, 10, 11, 20, 30) e del bonus di competenza ai livelli 1, 4, 5, 8, 9, 12, 13, 16, 17 e 20.
- [x] 1.2 Aggiungere al modulo il calcolo del valore di un tiro salvezza, di un'abilità, della Percezione passiva, dell'iniziativa, del bonus di uno strumento, del bonus di attacco e dei due valori da incantatore (`8 + bonus di competenza + modificatore` per la CD, `bonus di competenza + modificatore` per il bonus di attacco), restituendo l'assenza di risultato quando un ingrediente non è interpretabile o la caratteristica da incantatore non è scelta; verificare con test i tre livelli di competenza, i bonus vari, il bonus magico e ogni caso non calcolabile.
- [x] 1.3 Aggiungere `shared/dnd-rules.d.ts` e includere `shared` in `tsconfig.app.json`; verificare che `npm run build` completi il type-check e che il modulo sia importabile sia da `src/` sia da `server/` senza passo di build.

## 2. Modello dati e validazione sul server

- [x] 2.1 Estendere il vocabolario delle regole di campo con `'boolean'` e `'integer'` in `server/character-sheet-schema.mjs`, propagandolo a `defaultRow`, `validateCollection`, `normalizeRow` e `setValueError`; verificare con test che i due tipi siano accettati, che un valore del tipo sbagliato sia rifiutato e che partano dai predefiniti dichiarati.
- [x] 2.2 Rimuovere dal documento i valori derivati (`abilities.<k>.modifier`, `proficiencyBonus`, `passivePerception`, `initiativeModifier`, `savingThrows.<k>.value`, `skills.<k>.value`, `spells.saveDc`, `spells.attackBonus`) e introdurre il nuovo modello di `abilities`, `savingThrows`, `skills`, `level`, `initiativeMiscBonus`, `spells.spellcastingAbility` tipizzata, `spells.saveDcMiscBonus` e `spells.attackMiscBonus`; verificare con test che il documento iniziale abbia punteggi `10` e livello `1`, che sia valido e che una chiave rimossa venga rifiutata.
- [x] 2.3 Sostituire la validazione a validatore unico di `abilities`, `savingThrows`, `skills`, `hitDice` e `deathSaves` con una validazione per chiave, vincolando `hitDice.type` a `['', 'd4', 'd6', 'd8', 'd10', 'd12']`, i contatori dei salvataggi contro morte a `['0', '1', '2', '3']` e la competenza delle abilità ai tre livelli; verificare con test i valori del dominio e il rifiuto di quelli fuori dominio senza modifica del documento.
- [x] 2.4 Ridefinire la collezione `attacks` con i campi piatti elencati in `design.md` e rinominare `modifier` in `bonus` nella collezione `tools`; verificare con test che il documento iniziale sia valido, che una riga con campo sconosciuto venga rifiutata e che `critRange` parta da `20`.
- [x] 2.5 Aggiungere `character.sectionLocks` con le chiavi booleane `attacks` e `tools`; verificare con test che il documento iniziale resti valido, che le due chiavi siano obbligatorie e che una chiave extra venga rifiutata.
- [x] 2.6 Vincolare `spells.spellcastingAbility` al dominio `['', ...sei caratteristiche]`; verificare con test che una caratteristica valida sia accettata, che un testo libero sia rifiutato e che il documento iniziale parta da non scelta.
- [x] 2.7 Allineare i percorsi patch ammessi di `validatePatchOperation`: aggiungere i campi nuovi, `character.sectionLocks.*`, i bonus vari e i domini chiusi, e verificare che i percorsi dei valori derivati non esistano più; verificare con test che una patch su `character.abilities.strength.modifier`, `character.proficiencyBonus` o `spells.saveDc` venga rifiutata e che ogni percorso nuovo sia accettato con un valore valido.

## 3. Conversione dei documenti già salvati

- [x] 3.1 Implementare in `normalizeCharacterSheetData` la ricostruzione degli input: punteggio dal vecchio modificatore quando manca, livello dal vecchio bonus di competenza quando non è intero, competenza booleana delle abilità verso i tre livelli; verificare con test i casi punteggio presente, punteggio assente con modificatore presente, entrambi assenti e livello non interpretabile.
- [x] 3.2 Calcolare i bonus vari per differenza fra il vecchio valore della riga e il valore calcolato dai nuovi input, per tiri salvezza, abilità e iniziativa; verificare con test che un vecchio valore non intero lasci il bonus vari vuoto e che negli altri casi il totale ricalcolato coincida con il vecchio valore.
- [x] 3.3 Convertire attacchi (`bonus` → `attackBonus`, `damageType` → `damageDice`, `notes` → `description`, con le relative attivazioni), strumenti (`modifier` → `bonus`), tipo di dado vita e contatori dei salvataggi contro morte; verificare in `test/character-sheet-migration.test.mjs` che ogni valore inserito sopravviva e che i campi nuovi assumano i predefiniti.
- [x] 3.4 Convertire i valori da incantatore: riconoscere la caratteristica dal testo esistente confrontandolo con nomi e abbreviazioni delle sei caratteristiche, calcolare per differenza `saveDcMiscBonus` e `attackMiscBonus` quando è riconoscibile, e lasciare caratteristica non scelta e bonus vari vuoti quando non lo è; verificare con test il testo riconoscibile (valori identici prima e dopo), quello non riconoscibile (segnaposto, nessuna reinterpretazione) e il campo vuoto.
- [x] 3.5 Verificare la garanzia complessiva su un documento nel formato precedente: test che carica un `data_json` legacy con valori scritti a mano, lo normalizza e confronta **tutti** i totali visibili prima e dopo — modificatori, bonus di competenza, tiri salvezza, abilità, Percezione passiva, iniziativa e, quando la caratteristica da incantatore è riconoscibile, CD e bonus di attacco da incantatore — richiedendo che siano identici.

## 4. Service e proiezione sul token

- [x] 4.1 Sostituire in `server/character-sheet-service.mjs` la mappatura diretta di `character.initiativeModifier` con il ricalcolo dal modulo condiviso, innescato da una patch sul punteggio di Destrezza o su `initiativeMiscBonus`; verificare con test del service che entrambe le patch aggiornino il token, che le altre proiezioni restino invariate e che nessun campo di iniziativa venga scritto nella scheda.

## 5. Valori calcolati e competenze nel client

- [x] 5.1 Aggiornare `src/types/character-sheet.ts` con il nuovo modello di caratteristiche, tiri salvezza, abilità, iniziativa, attacchi, strumenti, `sectionLocks` e i domini chiusi; verificare che `npm run build` completi il type-check senza errori.
- [x] 5.2 Rendere il punteggio l'unico campo modificabile della coppia e il modificatore un valore di sola lettura, non un `input` disabilitato; verificare che il modificatore non entri nella tabulazione, che digitare `16` in Destrezza mostri subito `+3` e che non esista alcun percorso di scrittura del modificatore.
- [x] 5.3 Sostituire le caselle di competenza delle abilità con un indicatore a tre stati che cicla nessuna competenza, competente, esperto; verificare che sia azionabile da tastiera, che esponga lo stato corrente alle tecnologie assistive e che il valore della riga cambi di conseguenza.
- [x] 5.4 Mostrare come calcolati i valori di tiri salvezza, abilità, Percezione passiva, bonus di competenza e iniziativa, e aggiungere il campo bonus vari su tiri salvezza, abilità e iniziativa senza appesantire righe già dense; verificare che un bonus vari di `1` aumenti il totale esattamente di uno e che un livello non interpretabile mostri un segnaposto neutro invece di un numero inventato.
- [x] 5.5 Aggiornare `src/components/character-sheet/SpellsTab.tsx`: caratteristica da incantatore come select fra le sei caratteristiche o non scelta, CD del tiro salvezza e bonus di attacco come valori calcolati di sola lettura con il rispettivo campo bonus vari; verificare che con Carisma `16` e livello `5` la CD valga `14` e il bonus di attacco `+6`, e che senza caratteristica scelta entrambi mostrino il segnaposto.

## 6. Editor e righe compatte

- [x] 6.1 Implementare il componente di editor riutilizzabile su bozza locale, con conferma che emette una patch `set` per ogni campo cambiato, annullamento che scarta la bozza e rimozione dell'elemento; verificare che annullare non produca alcuna richiesta e che confermare invii una sola richiesta con le sole operazioni necessarie.
- [x] 6.2 Implementare l'editor dell'attacco con tutti i campi e le tre attivazioni previste dal delta spec, conservando i valori dei blocchi disattivati; verificare compilando un attacco completo, disattivando un blocco, confermando e riaprendo l'editor che i valori del blocco disattivato siano ancora presenti.
- [x] 6.3 Sostituire la riga di input degli attacchi con la riga compatta nome / bonus di attacco / danno-tipo e l'intestazione delle colonne, aggiungendo il comando a ingranaggio che riapre l'editor; verificare che i valori mostrati provengano dal modulo condiviso, che non siano modificabili nella riga e che l'ingranaggio sia raggiungibile da tastiera con etichetta accessibile.
- [x] 6.4 Applicare lo stesso editor e la stessa riga compatta a Strumenti e competenze; verificare aggiunta, modifica, rimozione e bonus mostrato per i tre livelli di competenza.
- [x] 6.5 Mostrare un segnaposto neutro quando il calcolo non è possibile e aggiornare le righe quando cambia un input a monte; verificare che portando il livello da `4` a `5` cambino insieme tiri salvezza competenti, abilità competenti, strumenti competenti e attacchi competenti, senza riaprire alcun editor e senza scrivere valori derivati nella scheda.
- [x] 6.6 Aggiungere il comando di blocco alle sezioni attacchi e strumenti, legato a `character.sectionLocks`; verificare che a sezione bloccata restino leggibili le righe e non siano disponibili aggiunta, apertura in modifica e rimozione, e che lo sblocco le ripristini.
- [x] 6.7 Conservare ed estendere gli identificatori stabili dei bersagli di tiro già introdotti in P0.4 (`data-roll-source`) su righe compatte degli attacchi, righe degli strumenti, riquadro dei dadi vita e riquadro dei salvataggi contro morte, e verificare che il riquadro Percezione passiva e le righe della tab Incantesimi non ne abbiano; verificare con un test del DOM che l'insieme dei bersagli corrisponda esattamente alla tabella della Fase B in `FEATURES_VTT.md`. La Fase A SHALL NOT eseguire alcun tiro.

## 7. Dadi vita e tiri salvezza contro morte

- [x] 7.1 Ridisegnare il riquadro dei dadi vita con il totale in alto, i rimasti in basso come valore principale e la select del tipo di dado accanto all'etichetta; verificare che la select offra soltanto lo stato «non scelto» e i cinque dadi e che il valore scelto sopravviva alla riapertura della scheda.
- [x] 7.2 Sostituire i due campi dei salvataggi contro morte con tre indicatori per riga, riempibili e svuotabili a mano; verificare che il contatore resti fra 0 e 3, che gli indicatori siano attivabili da tastiera e che lo stato sia leggibile dagli screen reader.
- [x] 7.3 Adattare `src/styles/character-sheet.css` ai nuovi riquadri, righe compatte, editor, indicatori di competenza, campi bonus vari e lucchetto, mantenendo la direzione "Grimorio di brace", il contrasto WCAG AA e gli indicatori di focus non tagliati; verificare su viewport desktop e stretto l'assenza di overflow orizzontale della finestra.

## 8. Coerenza degli strumenti esistenti

- [x] 8.1 Aggiornare `src/components/character-sheet/clearOperations.ts` perché riporti ogni campo al valore predefinito del proprio dominio — `10` per i punteggi, `1` per il livello, `false` per i booleani, il primo valore per gli insiemi chiusi — invece della stringa vuota; verificare che «Svuota scheda» produca solo patch accettate dal server e lasci il documento valido.
- [ ] 8.2 Verificare che editor, righe, indicatori, lucchetto e riquadri usino la catena patch/versione/SSE/debounce esistente senza modifiche al service: prova con Master e proprietario che una conferma dell'editor e un cambio di punteggio arrivino all'altra finestra senza reload e che la scheda risulti salvata dopo il debounce.

## 9. Verifica integrata e documentazione

- [x] 9.1 Eseguire `npm test`, `npm run build` e `npm run docs:check` e correggere ogni regressione; verificare che i tre comandi terminino con successo.
- [x] 9.2 Aggiornare i documenti `Docs/ai` raggiunti dai router frontend, backend e database per il modulo condiviso delle regole, il nuovo modello delle azioni e il lucchetto di sezione; verificare con `npm run docs:check` che la documentazione resti raggiungibile e coerente.
- [x] 9.3 Eseguire lo scenario manuale con Master e Player in due browser: punteggio a `16` e modificatore a `+3`, livello a `5` con propagazione del bonus di competenza, doppio click su Furtività per l'esperienza, bonus vari di `1`, aggiunta di un attacco dall'editor, blocco della sezione, riapertura di una scheda compilata prima della change e riavvio del server; verificare che nessun totale preesistente sia cambiato, che nessun dato sia perso e che i valori derivati non compaiano in `data_json`.
